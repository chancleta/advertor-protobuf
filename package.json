{
  "name": "AdvertorProtobuf",
  "version": "1.0.0",
  "description": "Protobuf repo",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "mkdir -p dist && pbjs -t static-module -w commonjs src/**/*.proto -o dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "make-dir-cli": "^2.0.0",
    "protobufjs": "^6.8.8"
  }
}
